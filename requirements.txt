# -------------------------------------------------
# PyTorch & TorchVision (Platform-specific)
# -------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu

# Linux (Docker/EB) - CPU only wheels
torch==2.4.1+cpu ; platform_system == "Linux"
torchvision==0.19.1+cpu ; platform_system == "Linux"

# macOS (Darwin) - regular wheels
torch==2.4.1 ; platform_system == "Darwin"
torchvision==0.19.1 ; platform_system == "Darwin"

# -------------------------------------------------
# Core Computer Vision
# -------------------------------------------------
opencv-python-headless==4.10.0.84

# 3D processing
open3d==0.18.0

# -------------------------------------------------
# API Deployment
# -------------------------------------------------
fastapi==0.112.2
uvicorn==0.30.6
python-multipart

# -------------------------------------------------
# Utilities & Helpers
# -------------------------------------------------
numpy
scipy
matplotlib
scikit-learn
tqdm
Pillow
python-dotenv
boto3